diff --git a/etc/contain.toml b/etc/contain.toml
index ab38b0f..82265c5 100644
--- a/etc/contain.toml
+++ b/etc/contain.toml
@@ -1,6 +1,6 @@
-pass_schemes = ["rand", "null", "tcp", "udp", "thisproc", "pty", "orbital", "display.vesa"]
+pass_schemes = ["event", "proc", "sys", "memory", "rand", "null", "tcp", "udp", "thisproc", "pty", "pipe", "shm", "time", "zero", "log", "initnsmgr", "orbital", "display.vesa"]
 sandbox_schemes = ["file"]
 files = ["file:/dev/null"]
-rofiles = ["file:/etc/passwd", "file:/etc/hostname", "file:/etc/localtime"]
+rofiles = ["file:/etc/passwd", "file:/etc/hostname", "file:/etc/localtime", "file:/etc/ld.so"]
 dirs = ["file:/tmp"]
-rodirs = ["file:/bin", "file:/ui"]
+rodirs = ["file:/bin", "file:/lib", "file:/ui", "file:/usr/lib"]
diff --git a/src/contain_thread.rs b/src/contain_thread.rs
index 4bbe965..b72ac30 100644
--- a/src/contain_thread.rs
+++ b/src/contain_thread.rs
@@ -2,7 +2,6 @@ use std::sync::{Arc, LockResult, RwLock, RwLockReadGuard};
 use std::thread::{self, JoinHandle};
 
 use event::{EventFlags, RawEventQueue};
-use libredox::call::setrens;
 use libredox::{flag, Fd};
 use log::{debug, error, warn};
 use redox_scheme::{read_requests, write_responses, Request, SignalBehavior};
@@ -11,6 +10,44 @@ use crate::contain_config::ContainConfig;
 use crate::filterscheme::FilterScheme;
 use crate::{ContainError, ContainResult};
 
+
+// FFI bindings to relibc namespace API (replaces deprecated SYS_MKNS syscall)
+pub mod ns_ffi {
+    extern "C" {
+        fn redox_get_ns_v0() -> usize;
+        fn redox_mkns_v1(names: *const libc::iovec, num_names: usize, flags: u32) -> usize;
+        fn redox_setrens_v1(rns: usize, ens: usize) -> usize;
+    }
+
+    const ERROR_BIT: usize = 1 << (usize::BITS - 1);
+
+    fn check(result: usize) -> Result<usize, syscall::Error> {
+        if result & ERROR_BIT != 0 {
+            Err(syscall::Error::new((result & !ERROR_BIT) as i32))
+        } else {
+            Ok(result)
+        }
+    }
+
+    pub fn mkns(scheme_names: &[&str]) -> Result<usize, syscall::Error> {
+        let iovecs: Vec<libc::iovec> = scheme_names.iter().map(|name| {
+            libc::iovec {
+                iov_base: name.as_ptr() as *mut _,
+                iov_len: name.len(),
+            }
+        }).collect();
+        check(unsafe { redox_mkns_v1(iovecs.as_ptr(), iovecs.len(), 0) })
+    }
+
+    pub fn getns() -> Result<usize, syscall::Error> {
+        check(unsafe { redox_get_ns_v0() })
+    }
+
+    pub fn setrens(rns: usize, ens: usize) -> Result<usize, syscall::Error> {
+        check(unsafe { redox_setrens_v1(rns, ens) })
+    }
+}
+
 pub struct ContainThread {
     config: Arc<RwLock<ContainConfig>>,
     namespace: usize,
@@ -29,17 +66,36 @@ impl ContainThread {
             ContainError::poison_error(e)
         })?;
 
-        let mut pass_scheme_ptrs = Vec::new();
-        for scheme in config_lock.pass_schemes.iter() {
-            pass_scheme_ptrs.push([scheme.as_ptr() as usize, scheme.len()]);
-        }
+        let pass_names: Vec<&str> = config_lock.pass_schemes.iter().map(|s| s.as_str()).collect();
 
-        let new_ns = syscall::mkns(&pass_scheme_ptrs).map_err(|e| {
+        // Open event queue and pipe BEFORE entering the new namespace, since
+        // these require root namespace access to the event: and pipe: schemes
+        let mut event_queue = RawEventQueue::new().map_err(|e| {
+            error!("could not open event queue");
+            ContainError::syscall_error(e)
+        })?;
+
+        let mut pipes = [0; 2];
+        match unsafe {
+            libc::pipe2(
+                pipes.as_mut_ptr(),
+                syscall::O_CLOEXEC as i32 | syscall::O_NONBLOCK as i32,
+            )
+        } {
+            0 => Ok(()),
+            -1 => {
+                error!("could not create pipe");
+                Err(ContainError::io_error(std::io::Error::last_os_error()))
+            }
+            _ => unreachable!(),
+        }?;
+
+        let new_ns = ns_ffi::mkns(&pass_names).map_err(|e| {
             error!("could not create namespace, {}", e);
             ContainError::syscall_error(e)
         })?;
 
-        setrens(-1isize as usize, new_ns).map_err(|e| {
+        ns_ffi::setrens(usize::MAX, new_ns).map_err(|e| {
             error!("failed to enter namespace, {}", e);
             ContainError::syscall_error(e)
         })?;
@@ -59,9 +115,9 @@ impl ContainThread {
             let scheme_handler = FilterScheme::new(&scheme_name, config_arc.clone());
             schemes.push((scheme_fd, scheme_handler));
         }
-        setrens(
-            -1isize as usize,
-            syscall::getns().map_err(|e| {
+        ns_ffi::setrens(
+            usize::MAX,
+            ns_ffi::getns().map_err(|e| {
                 error!("could not get namespace, {}", e);
                 ContainError::syscall_error(e)
             })?,
@@ -71,11 +127,6 @@ impl ContainThread {
             ContainError::syscall_error(e)
         })?;
 
-        let mut event_queue = RawEventQueue::new().map_err(|e| {
-            error!("could not open event queue");
-            ContainError::syscall_error(e)
-        })?;
-
         // Register for events before splitting into threads, to avoid scheme event race condition
         for i in 0..schemes.len() {
             let (scheme_fd, _) = &schemes[i];
@@ -91,23 +142,6 @@ impl ContainThread {
                 })?;
         }
 
-        // Create a pipe to request shutdown when the user command completes
-        let mut pipes = [0; 2];
-
-        match unsafe {
-            libc::pipe2(
-                pipes.as_mut_ptr(),
-                syscall::O_CLOEXEC as i32 | syscall::O_NONBLOCK as i32,
-            )
-        } {
-            0 => Ok(()),
-            -1 => {
-                error!("could not create pipe");
-                Err(ContainError::io_error(std::io::Error::last_os_error()))
-            }
-            _ => unreachable!(),
-        }?;
-
         debug!("pipes {:?}", &pipes);
         let [read_pipe, write_pipe] = pipes;
         let read_pipe = read_pipe as usize;
diff --git a/src/runner.rs b/src/runner.rs
index e09ad06..375fe69 100644
--- a/src/runner.rs
+++ b/src/runner.rs
@@ -4,12 +4,10 @@ use std::{
 };
 
 use libredox::call::waitpid;
-use libredox::error::{Error, EIO};
-use libredox::flag::O_RDONLY;
-use libredox::Fd;
 use log::{debug, error};
 
 use crate::{ContainConfig, ContainError, ContainResult, ContainThread, CONTAIN_EXEC_FAIL_EXIT};
+use crate::contain_thread::ns_ffi;
 
 /// Spawn and execute a command with no namespace changes.
 /// Used to execute a root shell.
@@ -46,29 +44,22 @@ pub fn run_contained(config: ContainConfig, command: Command) -> ContainResult<i
 
 /// List all schemes.
 fn list_schemes() -> ContainResult<Vec<String>> {
-    // get a list of all the schemes in the current namespace
-    let mut buf = [0; 4096];
-    let count = match Fd::open(":", O_RDONLY, 0) {
-        Ok(fd) => match fd.read(&mut buf) {
-            Ok(n) => n,
-            Err(e) => {
-                error!("Could not read root scheme");
-                return Err(ContainError::SyscallError(e));
-            }
-        },
-        Err(e) => {
-            error!("Could not open root scheme");
-            return Err(ContainError::SyscallError(e));
+    // Read scheme names from /scheme/ directory via std::fs (relibc path)
+    let entries = std::fs::read_dir("/scheme/").map_err(|e| {
+        error!("Could not read /scheme/ directory: {}", e);
+        ContainError::io_error(e)
+    })?;
+    let mut schemes = Vec::new();
+    for entry in entries {
+        let entry = entry.map_err(|e| {
+            error!("Could not read scheme entry: {}", e);
+            ContainError::io_error(e)
+        })?;
+        if let Some(name) = entry.file_name().to_str() {
+            schemes.push(name.to_string());
         }
-    };
-    Ok(String::from_utf8(buf[0..count].to_vec())
-        .map_err(|_e| {
-            error!("Could not convert schemes to uft8");
-            ContainError::SyscallError(Error::new(EIO))
-        })?
-        .split_ascii_whitespace()
-        .map(|s| s.to_string())
-        .collect())
+    }
+    Ok(schemes)
 }
 
 /// Validate the config.
@@ -158,7 +149,9 @@ pub fn run_in_namespace(mut command: Command, namespace: usize) -> ContainResult
     }
     let pid = pid as usize;
     if pid == 0 {
-        syscall::setrens(namespace, namespace).map_err(|e| {
+        // Child must enter the restricted namespace explicitly, as fork()
+        // creates a new context that does not inherit namespace bindings.
+        ns_ffi::setrens(namespace, namespace).map_err(|e| {
             error!("child failed to enter restricted namespace, {}", e);
             ContainError::syscall_error(e)
         })?;
